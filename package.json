{
  "name": "create-docker-db",
  "version": "0.3.4",
  "description": "A CLI to easily create DBs for local development using Docker Compose",
  "keywords": [
    "database",
    "docker",
    "development"
  ],
  "author": "Aryan Prince <hello@aryanprince.com> (https://aryanprince.com)",
  "homepage": "https://github.com/aryanprince/create-docker-db",
  "bugs": {
    "url": "https://github.com/aryanprince/create-docker-db/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aryanprince/create-docker-db.git"
  },
  "license": "MIT",
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "create-docker-db": "dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "bun src/index.ts",
    "clean": "rm -rf node_modules && rm -rf pnpm-lock.yaml && rm -rf dist",
    "clean:dist": "rm -rf dist",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "tsc",
    "test:build": "tsc -p tsconfig.test.json",
    "test": "pnpm run test:build && node --test .test-dist/test/*.test.js",
    "test:integration": "RUN_DOCKER_TESTS=1 pnpm run test",
    "ci": "pnpm run format:check && pnpm run lint && pnpm run test && pnpm run build",
    "release": "pnpm run ci && pnpm release-it",
    "prepublishOnly": "npm run ci"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.5",
    "@types/node": "^22.20.2",
    "prettier": "^3.9.6",
    "release-it": "^20.2.1",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "commander": "^12.1.0",
    "yaml": "^2.9.1"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.28.1",
      "undici": "^7.29.0"
    }
  }
}
