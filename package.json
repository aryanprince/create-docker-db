{
  "name": "dev-db",
  "version": "0.0.1",
  "description": "A CLI to easily create DBs for local development using Docker Compose",
  "keywords": [
    "database",
    "docker",
    "development"
  ],
  "author": "Aryan Prince <hello@aryanprince.com> (https://aryanprince.com)",
  "homepage": "https://github.com/aryanprince/dev-db",
  "bugs": {
    "url": "https://github.com/aryanprince/dev-db/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aryanprince/dev-db.git"
  },
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "bin": {
    "dev-db": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "pnpm build && pnpm link --global && dev-db",
    "ci": "pnpm run format:check && pnpm run check-exports && pnpm run build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check-exports": "attw --pack . --ignore-rules=cjs-resolves-to-esm"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.16.4",
    "@types/inquirer": "^9.0.7",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "inquirer": "^11.0.2"
  }
}
